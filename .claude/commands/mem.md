# /mem

Run the Evo-Lite low-frequency handover workflow.

## Intent

- confirm backlog and focus state
- prepare the next-session focus
- bump version and tag when appropriate

## Important

- `/mem` is for handover and release-style transitions, not ordinary bug-fix closure.
- Canonical workflow semantics still live in `.agents/workflows/mem.md`.
