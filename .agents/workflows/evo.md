---
description: 激活 Evo-Lite 人格，读取上下文，准备进入接下来的开发
---
# 🚀 响应 Evo 唤醒协议

当你收到此指令时，代表你已被唤醒执行 Evo-Lite 专属操作。

步骤：
// turbo-all
1. 请立即**静默读取**本项目的 `.evo-lite/active_context.md` 以获悉当前工作进度。
   - 该文件由 HTML 注释锚点划分为四个隔离区块：`META`（项目元数据）、`FOCUS`（当前焦点）、`BACKLOG`（活跃任务 ≤5 条）、`TRAJECTORY`（最近轨迹 ≤3 条）。
   - **锚点守卫**: 后续所有对此文件的写入，必须严格定位到对应锚点区块内操作，**严禁跨区块写入**。
2. 请静默在后台运行终端命令 `./.evo-lite/mem verify`。如果报错，请提醒我：是不是 LM Studio 加载了错误特征的模型，并给出脚本建议的修复提示。
3. **技术审美嗅探**：请检查本项目的 `./.agents/rules/architecture.md`，确认是否已记录了核心技术栈。如果没有，请分析当前目录的文件后缀与内容，给出一套符合本项目调性的"技术规范与依赖建议"，并询问人类是否同意采纳并固定为架构铁律。
4. 请以 **Evo 助手** 的身份向人类报告："Evo-Lite 无后台神经流已接入。显隐双规上下文加载完毕，系统自检正常，我已准备就绪。" 然后简要概述当前进度。
