# 🧠 Evo-Lite Active Context (EvoRouter)


> ⚠️ **框架已热更新**: 检测到核心引擎升级，原有的历史进度（如 `active_context.md`）已备份至 `.bak`。**请 AI 助手立即阅读 `active_context.md.bak` 以核对并恢复最新的开发进度。** 在完成手动融合与清理后，请删除此警告并清理 `.bak` 文件。
> **更新时间**: 2026-03-10
> **项目状态**: v1.4.8 离线化终结者：内置 Embedding 离线模型包，实现零网络环境下的完整 RAG 启动；并彻底重构了跨模型迁移管道与初始化向导。



## 1. 🎯 核心目标
- **工程愿景**: 持续打磨和迭代 `create-evo-lite` 骨架代码，使其成为 Agentic Workflow 的终极“无感高压治理挂件”。确保其具备高度的稳定性、跨平台容错性以及极低的侵入感。

## 2. 🚧 当前进度与任务
- [x] 完成了 npm 发布与 `index.js` CLI 初始化剧本。
- [x] 完成了核心防线 v1.0.3 的建设：Offline 编译回退与 Ctrl+C 取消捕捉。
- [x] 完成了 v1.1.0 重构：扩充 `stats/forget/compact/import/export` 五大本地指令，并实现了与 LM Studio 动态模型嗅探。
- [x] 完成了 v1.2.0 加固：解决 SQLite 并发 Locked、大记忆体 Compact 溢出 (Map-Reduce)、并防拆了 Shell 的 `--file` 传参机制。
- [x] 完成了 v1.3.0 增强：注入时空锚点 (Space-Time Git Hashes)，增加 AI 智能体戒律法则。
- [x] 完成了 v1.3.1 架构升维：剥离 `compact` 函数内部对本地小语言模型的依赖。改由 CLI 输出 `.evo-lite/MEMORIES_TO_COMPACT.md` 唤醒协议交接给高智商宿主 IDE Agent 代为进行深度的中文总结提炼。
- [x] 完成了 v1.3.2 热修复：修复 `compact` 数据结构缺少 ID 字段导致批量交接失败的隐怀，并全盘演练验收。
- [x] 完成了 v1.3.3 优化：彻底剥离已弃用的本地对话模型嗅探逻辑，并实装 CLI 初始化时的版本号展示与 package.json 注入。
- [x] 完成了 v1.3.4 规范：新增版本发布打标签协议 (Release Tagging Protocol)。
- [x] 完成了 v1.3.5 热修复：修正 API 探测成功后未及时关闭 socket 导致的延迟超时误报。
- [x] 完成了 v1.3.6 补完：同步补齐了实例层 (`.evo-lite/`) 的记忆蒸馏消歧规则，并固化版本。
- [x] 完成了 v1.3.7 铁壁：将“记忆蒸馏规范”从纯提示词软约束，升级为 `memory.js` 底层的**程序级致命防线**，彻底拦截无溯源与错误区间省略格式的劣质长文本入库，并同步精简了 `ACTIVATE_EVO_LITE.md` 的提示词长度。

- [x] 完成了 v1.3.14 洗盘协议 (Data Washing)：针对项目根目录 `.evo-lite/memory.db` 实现了初始化阶段交互式脱机格式清洗重组引导 (配合 \`/wash\` 协议)。
- [x] 完成了 v1.3.15 发版静默：创建 `.npmignore` 隔离 \`memory_dump\` 与历史脏数据。
- [x] 完成了 v1.3.16-1.3.21 架构全面洗练与抗失忆加固：
  - **语义剥离**: 数据库操作专属别称缩写收敛归位为 \`mem\`，与最高唤醒词 \`/evo\` 形成上下文互不污染。
  - **动态钩子注入**: 放弃单纯的 Markdown 冗长说教，改为在底层的 Node.js \`memory.js\` 写入时主动回显终端告警，**强制**督促完工的智能体覆写本上下文。
  - **白皮书沉淀**: 将这套防范智能体闭环失忆和偷懒的法则凝结为了 \`docs/AI_AGENT_DEFENSE_ARCHITECTURE.md\`。

- [x] 完成了 v1.3.25 闭环：
  - **热更增强**: 实现了 \`active_context.md\` 的热备份 (.bak) 与模板强制覆写。
  - **AI 视界校准**: 在新模板中注入 AI 引导标签，强制接管的智能体通过读取 \`.bak\` 自行恢复进度。
  - **代码洗练**: 优化了 \`index.js\` 的结构，移除了冗余逻辑，修复了文本转义与正则表达式缺陷。
- [x] 完成了 v1.3.26 规范回滚与增强：
  - 由于 AI 在过度精简版的协议中容易“发散”并遗忘规则，全面恢复了 \`ACTIVATE_EVO_LITE.md\` 的长篇详细护城河（包括强制交接格式、CLI 指令大全、以及硬拦截规则的严厉警告），并同步了最新的 \`.bat\` 兼容特性。
- [x] 完成了 v1.3.27 增强：新增了针对智能体的状态保持工作流 \`/mem\` 协议，统一规范和强制了如何变更显隐状态以及 Commit。
- [x] 完成了 v1.3.28 架构修复：重构了框架热更新时的“内容融合 (Fusion)”逻辑。纠正了上个版本中“误将模板覆盖旧上下文”的致命 Bug，现在会完全保留用户的 \`active_context.md\` 原文，仅在顶部注入融合指引。
- [x] 完成了 v1.3.29 鲁棒性增强：重构了脚手架 (\`index.js\`) 与 CLI (\`memory.js\`) 的模型探活逻辑。废弃了仅能检查端口连通性的 GET 探测，改为使用真实 POST 请求获取向量指纹。解决了“服务器开启但模型未加载却显示验证通过”的误报问题。
- [x] 完成了 v1.3.30 交接约束增强：遵循人类最新指令，在 `ACTIVATE_EVO_LITE.md` 中强化了交接工作流。增加了要求修改 `package.json` 的版本规范，并且明确要求录入经验时的 `git rev-parse` 回调格式。
- [x] 完成了 v1.4.0 架构升维与协议收敛：
  - **规则下沉**: 集成 `eov` 高纪律性协议，由文档引导升级为 `.agents/rules` 系统级硬约束。
  - **引擎加固**: `index.js` 支持配置提取、热更新 Fusion 融合与 Silent 模式。
  - **依赖鲁棒**: 实现了 `tar` 方案的脱机依赖注入兜底。
- [x] 完成了 v1.4.0 全盘架构复盘（三次迭代演进审计）。
- [x] 完成了 v1.4.0 新项目初始化与结构注入验证。
- [x] 完成了 v1.4.0 官方文档 (README/CHANGELOG) 的同步更新。
- [x] 完成了 v1.4.1 细节修补：优化了 Antigravity 环境下的初始化提示，增强了 Reranker 的真实探活逻辑。
- [x] 完成了 v1.4.2 规范固化：在 `/mem` 协议中强制实装版本小步跃迁 (Bump Version) 流程。
- [x] 完成了 v1.4.3 强化：完善了 /mem SOP 中的“必须”强制性，并确保任务清单闭环。
- [x] 完成了 v1.4.4 启动与环境同步：通过 `/evo` 协议激活系统，自检发现并清理了模板组件 (`templates/`) 的未提交改动，重新进入稳定开发状态。
- [x] 完成了 v1.4.5 协议精进：针对 `/mem` 工作流增加了“精确提取短哈希”步骤，以提升向量记忆溯源的准确性与便捷度。
- [x] 完成了 v1.4.6 跨时代革命：彻底剥离 LM Studio。集成 `@xenova/transformers`，在不引入额外进程的情况下，将底层引擎升级为了基于本地缓存的 ONNX 架构，实现了 Bi-Encoder 嵌入 (512维) 与 Cross-Encoder 精排的双核本地处理。
- [x] 完成了 v1.4.7 护城河加固：恢复了 `memory.js` 的强容量熔断防线；考虑到 ONNX I/O 在 SQLite + Vec 下的极致榨取表现，将触发 `compact` 的水位线从 `15条` 提升到了 `30条`。
- [x] 完成了 v1.4.7 AI 指令集升级：补齐并分发了 `evo-lite.md` 的底层防线（加强 YAGNI 复用原则、增加 Atomic Commits 与 Windows 防火机制）。
- [x] 完成了 v1.4.8 离线化终结者：打包了 15MB 的 `bge-small-zh-v1.5` 离线 ONNX 模型；修复了初始化时序 Bug、指纹死锁、Import 守卫误杀；重写了零配置向导与全自动跨模型迁移管道。

## 3. 📝 下一步行动指南 (Next Actions)
- [ ] 开展对纯 Node.js ONNX RAG 架构的长周期稳定性与内存泄漏排查。
- [ ] 验证 `/mem` 协议在跨会话状态继承中的表现。
- [ ] 考虑下一步增加对 Python/Go 等非 Node 环境的轻量化适配支持。

## 4. 📌 架构备忘录 / 搁置区 (Backlog & Ideas)
- 这里用于存放触发的灵感、架构备忘或暂时搁置低优先级的任务，防止遗忘且不干扰主线。