---
id: "c8ec3bb_d92ebc23"
timestamp: "2026-06-16T14:29:24.920Z"
type: "task"
namespace: "prose"
tags: []
---

## 实现细节 (Implementation)
[rc-closure-phase2-dx]
RC closure phase 2 delivers spec:rc-closure-phase2-dx end-to-end. Seven tasks landed.

P0 sync-runtime: templates/cli/ is now the canonical CLI source. `mem sync-runtime` copies templates → .evo-lite/cli/ and writes a SHA-256 lock at .evo-lite/generated/runtime-mirror.lock.json. `verify` hard-errors when the runtime mirror drifts from the lock. Killed the dual-maintenance burn that wasted one whole verify cycle this session.

P2a MCP cache: mcp-server now uses freshRequire(modulePath) — mtime-checked require.cache invalidation. `mem architecture scan` writing 11 modules to disk now actually propagates to mcp__evo-lite__evo_architecture_status without a server restart.

P1b R008 amnesty + backfill: new `mem plan archive-evidence --backfill` walks raw_memory and links archives to tasks by task: regex + bare [task-suffix-tN] tag. Plan frontmatter `r008Exempt: true` skips R008 for legacy plans. Dogfood R008 count: 52 → 0.

P2b context auto-refresh: `mem context auto-refresh` re-derives focus from active plan + prunes backlog entries whose linked task is implemented. Idempotent.

P2c architecture where: `mem architecture where <file>` returns module id + role + linked task ids. Reverse lookup that used to need an inline node script.

P1a plan-for-in-flight: `verify` now detects uncommitted edits on ARCH/PLAN sources + empty backlog and emits `mem plan new <slug> --from-diff` guidance. New scaffolder writes spec + plan stubs.

P3 hook DX + CRLF: `mem hook diff`, `mem hook last`, `mem hook install --explain`. .gitattributes normalizes EOL to silence Windows CRLF warnings on the runtime mirror.

Tests: T17-T23 added. Full + governance suites pass.

## 架构决策 (Architecture)
未记录
