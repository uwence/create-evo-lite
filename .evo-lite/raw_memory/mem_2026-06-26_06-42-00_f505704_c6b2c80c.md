---
id: "f505704_c6b2c80c"
timestamp: "2026-06-26T06:42:00.590Z"
type: "task"
namespace: "prose"
tags: []
---

## 实现细节 (Implementation)
[release-closure-rc2]
Implements plan:release-closure-rc2 (spec:release-closure-rc2). Fixes 3 release blockers an external review found in 2.0.10 + the harness gap that hid them + doc drift. task:release-closure-rc2-t1: installRuntimeDependencies copies manifest+lockfile before the skip-install return so 'cd .evo-lite && npm ci' recovery works (guard T18f). task:release-closure-rc2-t2: writeRuntimeManifest writes evo-lite-version.json=SELF_VERSION, getRuntimeVersion prefers it so MCP advertises 2.0.10 not the pinned 1.0.0 manifest (guard T18g; index exports <REDACTED:high_entropy>). task:release-closure-rc2-t3: regenerated root package-lock.json to 2.0.10 (guard T18h). task:release-closure-rc2-t4: test scope 'all' now runs governance+integration so npm test/CI exercise the guards (root cause the blockers shipped green); reconciled release-gate header, phase-1 spec architecture line, and the false 'VERIFIED ALREADY FIXED' item-2 verdict in spec:release-closure-patch. npm test green both suites exit=0.

## 架构决策 (Architecture)
未记录
