---
id: "19b66c5_3ccd074e"
timestamp: "2026-09-02T12:44:28.825Z"
type: "task"
namespace: "prose"
tags: []
---

## 实现细节 (Implementation)
[Zvec070MeasurementPhaseClosed]
[zvec-win-unicode-containment] 测量阶段 CLOSED。Step 2C(非 ASCII 桥)经复审 APPROVED,PR #62 以两父 merge 合入 main@19b66c5(复审 head 778e03a,release-gate run 33629562249 completed/success 6/6)。Step 1(触发边界)· 2A(安装/加载/原生 smoke)· 2B(真实 adapter 合同)· 2C(非 ASCII 桥)四步全部结束。

有界结论:在本机 Windows 11 10.0.26200(能复现 0.6.0 native fail-fast 的环境)、预登记的非 ASCII path shape 日本語-한국어 @ collectionPathLen 149 上,@zvec/zvec@0.7.0 满足 memory-index-zvec.js@d3ffe05d 实际消费的 Node API、返回值形状与跨进程持久化合同;node 22.22.2 与 24.18.0 各 SATISFIED,19/19,零失败,装置身份 4/4 MATCH。生产代码变更为零。

本轮(778e03a)修的是装置缺陷,不是被测对象:共享分类器抽出后,bridge 仍按 Step 1 的 stderr marker 协议读 jsError,而它自己的 child(adapter-txn.js)的 JS-error 通道是证据文件里的 ev.error。于是可捕获异常以「status 1 / 无 jsError」到达分类器,被正确地判为 FAIL_FAST_REPRODUCED。突变对照实测:一个只是抛了普通异常、从未崩溃的对照,在修复前足以让 0.7.0 跑满三相并产出 SATISFIED。修法是 caller 自己映射 transport(jsError = Boolean(ev && ev.error)),并把分类器的输入对象写进 record(control.classifierInput / control.error)——旧 record 只有结论没有输入,所以错误映射在读证据时不可见。第三个终止态(跑完但 allPassed:false)对对照不可达,边界写进注释而非硬改冻结语义。工件地位分四层:results-bridge/=authority,results-negative-control/=defect proof,results-out-of-regime/=非权威诊断,results-ci-image/=该镜像不复现的环境证据。

仍然严格不授权:升 pin / 进 RUNTIME_DEPENDENCIES / optional→mandatory / 默认翻转 / 放松 containment / 任何发布决定。containment 反而因本轮多一条独立理由:该故障的可观测性依赖尚未隔离的环境维度(GitHub windows-latest 镜像在同一 colPathLen 149 上根本不复现 control)。

下一步是 0.6→0.7 的 Upgrade Decision Record,对上述六项分别裁定。UDR 是一道新的独立裁决门,不继承 Step 2C 的 SATISFIED,尚未授权、尚未开始。

## 架构决策 (Architecture)
未记录
