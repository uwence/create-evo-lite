---
id: "2bf2424_6d3fb91b"
timestamp: "2026-03-20T17:14:11.681Z"
type: "task"
tags: []
---

## 实现细节 (Implementation)
[InitStartupHardening]
Fixed two concrete initialization regressions found from a fresh generated project. First, the scaffold installer now installs tar alongside better-sqlite3, sqlite-vec, and @xenova/transformers, so a new workspace no longer crashes on first verify because models.js requires tar for offline model extraction. Second, the initial active_context trajectory placeholder now uses a stable init label instead of leaking {{DATE}} into square brackets, and the /evo workflow was tightened so brand-new placeholder projects are reported as initialization state instead of being over-interpreted as real focus or architecture facts.

## 架构决策 (Architecture)
未记录
