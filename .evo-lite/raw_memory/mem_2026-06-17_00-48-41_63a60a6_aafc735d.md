---
id: "63a60a6_aafc735d"
timestamp: "2026-06-17T00:48:41.973Z"
type: "task"
namespace: "prose"
tags: []
---

## 实现细节 (Implementation)
[rc-closure-phase3]
RC closure phase 3 — last-mile consistency fixes before production dogfood:

1. hooks.js: post-commit hook now detects .evo-lite/raw_memory/*.md churn (EVIDENCE_CHANGED) and runs 'plan archive-evidence --backfill' + 'plan scan' automatically. R008 archive evidence loop is now no-touch — meta-commits that ship raw_memory archives refresh the evidence map before plan scan reads it.

2. planning.js scaffoldPlanStubs: plan frontmatter now emits 'id: plan:<slug>', 'format: superpowers', 'status: draft' alongside 'linkedSpec'. 'plan new' output now satisfies 'plan lint' contract. T23 test extended to assert the id line.

3. sync-runtime.js --check: exits non-zero on any status !== 'ok' (was only 'drifted'). 'no-lock' now also fails — first-run CI no longer false-passes.

Verified: full test suite + governance scope (T8–T23) green after sync-runtime + hook install.

## 架构决策 (Architecture)
未记录
