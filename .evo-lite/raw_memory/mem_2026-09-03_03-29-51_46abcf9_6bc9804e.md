---
id: "46abcf9_6bc9804e"
timestamp: "2026-09-03T03:29:51.658Z"
type: "task"
namespace: "prose"
tags: []
---

## 实现细节 (Implementation)
[ProductSupportScopeGateClosed]
[A0 product-support-scope] gate 三阶段全部冻结并合入 main@46abcf96(PR #64 两父 merge,复审 head aa59c36c,release-gate run 33711008814 completed/success 6/6)。docs-only:相对 main@af590b8 的 tree delta 只有新增 docs/validation/product-support-scope.md 一个文件,生产代码零变更。

阶段:Stage 1 主语 FROZEN e1084f01(4 轮复审)· Stage 2 判据 FROZEN 3dd343cb(5 轮复审)· Stage 3 裁决 FROZEN aa59c36c(2 轮复审)。判据冻结于声明之前四个 commit,故「先看答案再定判据」由 git 历史排除而非作者自陈。

四项裁定:P1 S_PRODUCT declaration = GREEN;P2 V_PRODUCT = DEFERRED(无候选 + 缺 B3);P3 coverage justification = DEFERRED(DF3 输入不完整);P4 enforcement = DEFERRED(无逐 segment 的 mode 候选)。本轮唯一的实质正授权只有一句:S_PRODUCT declaration is authoritative。P1 是四个节点里唯一一个所需 authority 恰好只有所有者能单独给出的那个;其余三个卡住的原因各不相同,但没有一个是「测量不够」。

已成为 authority 的所有者声明(B1,2026-09-03):OS 支持 win32 与 linux、不支持 darwin;arch 支持 x64、不支持 arm64;两轴共用理由「先不拓展平台,加快开发进度」。Node 采用静态枚举,支持 20/22/24,其余 major(含 21/23 与 25 及以后)全部不支持,理由是「不同 agent 宿主钉定的 Node 版本不一致」—— 该理由只能由所有者给,也正是「把 floor 抬到 22」对本产品错误的原因。

两条以后会反复用到的事实:better-sqlite3@12.11.1 的 node prebuild ABI 集合为 {127,137,141,147},不含 115,故当前 S_PRODUCT 的 Node20 × {win32,linux} × x64 上无 prebuild,而该包 install script 为 prebuild-install || node-gyp rebuild --release,这些 cell 需要本地 native toolchain —— 承重的是「该包该版本 + dependency-installability 这一条 predicate + 当前 release assets」,不是 Node 20 的一般性质。以及:win32 × Node20 现在在 S_PRODUCT 之内、却在 release-gate 五格 S_GATE 之外,因此 V_PRODUCT 不可能既等于那五格又覆盖承诺。

canonical contract 创建条件已冻结:docs/specs/<product-support-contract>.md 现在 NOT YET;允许创建 iff P1 GREEN ∧ P2 GREEN ∧ P3 ∈ {GREEN, BOUNDED},且 P3=BOUNDED 时 bounded conclusion 必须进入 contract 本体;P4 不是前置条件。现在建一个只装 S_PRODUCT 的合同,会制造「artifact identity = 完整合同 / contents = 四分之一」,正是本 gate 要消除的形状。

对 UDR 的影响为零解阻:本轮只满足 UDR 的 A0 中 S_PRODUCT 那一个 component,不等于 A0 已 satisfied;V_PRODUCT 与 coverage justification 仍缺,故 UDR 的 D1 仍是 DEFERRED。

对未来的边界:本次 P2 = DEFERRED 是 aa59c36c 当时 authority 状态下的正确历史裁决。将来有了 B3 与 V_PRODUCT candidate 之后,应当做一次新的 consumption / re-adjudication record,而不是回头把本文档里的 DEFERRED 改成 GREEN,否则 evidence history 会被重写。后续的 V_PRODUCT gate 是一条独立 gate,不是「A0 Stage 4」;A0 已冻结,不重开。该 gate 的第一主语应是 B3(一个 cell 凭什么算 verified),而不是先选 CI 格子 —— 否则会变成「先看见现有五格,再设计一套刚好让五格成立的 verified 定义」,与本工作线一路在防的形状同型。

仍然不授权:V_PRODUCT / coverage justification / enforcement 变更 / canonical spec / UDR A0 satisfied / D1 重裁 / 任何实施或生产变更。

## 架构决策 (Architecture)
未记录
