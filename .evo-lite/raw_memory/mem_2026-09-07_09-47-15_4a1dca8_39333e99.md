---
id: "4a1dca8_39333e99"
timestamp: "2026-09-07T09:47:15.942Z"
type: "task"
namespace: "prose"
tags: []
---

## 实现细节 (Implementation)
[architecture-scan-coverage]
架构扫描覆盖率缺陷收口：装置不再把「没看见」报成「没问题」。PR #74 以两父 merge 合入 main@4a1dca8（parents 7899dcf + b5e14da），release-gate 在 exact head b5e14da 上 6/6 PASS（ubuntu node 20/22/24、windows node 22/24、containment + release enforcement contract evidence，run 34106366715 的 head_sha 已核对逐字符相同）。

缺陷来源不是我们主动巡检发现的，是 owner 打开子巢 wiki 时撞见的：hungersnakegame8 的「项目全貌」把一个 Three.js 3D 贪吃蛇描述成有「CLI 入口 / Agent 与工作流 / 文档与规划」三个模块，把它的 package.json 标注为 "Main CLI entry point and command router"，结尾写「未发现需要处理的问题」。

根因是三层硬编码，全部是 create-evo-lite 的自画像：scan-native.js 的 WALK_TARGETS 不含 src/、INCLUDE_EXT 不含 .ts、infer-modules.js 的 12 条 MODULE_RULES 里 10 条指向只存在于母仓的 templates/cli/* 路径。关键在于第二级后果：源码压根没被走到，所以也不会被判为 unclassified，于是连 R007 都不触发——扫描返回一个干净的 IR，而它什么都没看见。装置的绿色不代表它看过。

爆炸半径实测（读子巢自己的 IR，不是推断）：CodePLC 181 个源码文件 → IR 收录 0 → warnings 0；hungersnakegame8 20 → 0 → 0；hungersnakegame7 是空项目；hungersnakegame4 连 IR 都没有。母仓看起来正常，因为那张地图就是母仓。这不是随机偏移，是每个子巢 100% 同形复现的产品缺陷，与项目类型无关。

本次只修 detection half，不修模块地图，commit 正文明写 "This commit does not fix the module map. It makes the gap audible."。权威改用 git ls-files——刻意不再加第二份硬编码目录清单，因为重复那个错误正是沉默的来源；无 git 时报 state:"unknown" 并给理由，不猜。排除 .evo-lite/ 是因为子巢确实把运行时纳入版本控制（hsg8 109 个 .js、CodePLC 96 个，均在子巢仓内实测），那是本工具自己的代码。SOURCE_EXT 只是上报启发式而非项目定义：它判错会点错文件，但永远不会恢复沉默。

TDD 与突变负控：RED 红在「the IR must carry a coverage report」，其余 149 条 governance 测试全过；第一次红是 TypeError，按纪律改成断言后重跑。因为 RED 停在第一个断言、R014 那半从未被证明能红，补了两次突变——摘掉 checkR014 只红 GUARD:a-r014-emitted（且 coverage 测量仍绿，证明两半独立），让 R014 无条件触发只红 GUARD:b-no-r014 且别处不红。负控 (b) 用 index.js 这个「被覆盖的」源码文件（covered 1 / uncovered 0），使「永远告警」的假实现无法通过；(c) 断言无 git 时不得把 unknown 冒充 uncovered。

预期留存的 finding：母仓自身 R014 covers 114/131，17 条落在 .github/hooks/、.codex/hooks/、docs/validation/fixtures/。这条是对的，不得为让 CI 好看而调没——调没它就是本次修复要治的病。owner 明确裁定 EXPECTED / DO NOT TUNE AWAY。

子巢侧：hungersnakegame8@4b78cce 接收 5 个基因（4 个 architecture + planning/scan.js），回滚标签 evo-nurture-pre-2.4.0-20260907T090423。第 5 个是 dry-run 点名后才发现的：PR #73 的反向链接修复今天 07:50Z 才进 main 而该子巢 02:58Z 已 nurture 过，那正是它自己报了两次的缺陷。子巢实测 coverage 0/20，wiki 的「未发现需要处理的问题」变成「未归属：1 项治理提醒」。owner 决定其余三个子巢固定版本、不再 nurture。

集成策略用 merge 而非 rebase，理由是 provenance：168a978 已在 main 上把 d3752b9 记成 nurture subject（Nurtured from fix/architecture-scan-coverage@d3752b9 / Child-side closure: hungersnakegame8@4b78cce），rebase 会让 main 的持久记录指向一个 lineage 里不再存在的 SHA——与 #71 的 evidence-bound SHA 同型，只是这次绑定它的是 nurture provenance。合并后逐项验证：d3752b9 与 7899dcf 均为祖先、behind 0、相对 main 的 product diff 仍精确是那 8 个文件 +338/-4、8 个文件的 blob hash 与 d3752b9 逐字节相同、4 对 .evo-lite↔templates 镜像 hash 相同。合入后 d3752b9 / b5e14da / 7899dcf / 168a978 四个 SHA 全部仍可达。

诚实边界，两条不得升级为「已证明」：本地全量套件跑在 d3752b9 而非 b5e14da（merge 只带进 main 上已存在的 state 文本，exact-head 验证由 release-gate 承担，现已 6/6 通过）；hungersnakegame8 的 T-hive-scaffold-lock 红（Cannot find module <root>/index.js，母仓脚手架入口，子巢从来没有）有三条旁证指向既有失败——测试在子巢 HEAD 就存在、harness.js 未被 nurture、失败路径 governance.js:328 落在两个 diff hunk 之外——但未回退实测前置状态，状态维持 likely pre-existing / NOT independently established，owner 裁定不为把标签从 likely 改成 proven 而开工作线。

owner 同时 PARK 了三项，均不得因本次收口而自动解冻：wiki 正文如何强调未覆盖属于 presentation policy（DEFER）；模块地图外置为项目级配置是第二阶段 architecture redesign，需独立授权（PARK）——不得把 detection fix 自动升级成 module-boundary framework；其余三子巢的显式 pin 记录不做，因为没有真实失败源于「缺 pin record」，爆炸半径为零不是现在做它的理由。

## 架构决策 (Architecture)
未记录
