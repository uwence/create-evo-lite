---
id: "ac58d9e_bb66e42f"
timestamp: "2026-09-06T09:24:40.838Z"
type: "task"
namespace: "prose"
tags: []
---

## 实现细节 (Implementation)
[pr71-integration-closure]
#71 收口:gitignore 规则下发、drift 折叠显示、verifier budget、2.4.0 版本号。
PR #71 合入 main@ac58d9e(两父 merge),CI release-gate 6/6 绿 @ 82700c8。

处理方式:**merge main 进 #71,不 rebase**。#71 携带真实 verification evidence,其记录绑定
commitSha 3b6f464;rebase 会重写提交身份而让 evidence 指向分支上已不存在的 sha。合并保留
801d013/01b7fd5/3b6f464/440733b/9113962 为集成 head 的祖先,provenance 由构造成立。
这与本工作线一贯的原则一致:证据绑定的 commit 不是可随意重写的装饰。

冲突面只有 test/governance.js 及其镜像,且是同文件不同语义区段:#71 追加
gitignore-rule-sync / nurture-gitignore / finding-digest,#72 改的是 manual-attestation
区域(T51/T52b)。实际合并零冲突,两侧测试合并后全部通过。

合并前发现并补齐的一处证据缺口:native-lite 四条 machine 判据中有两条 live STALE。
归因清楚且不是合并造成的 —— merge 未触及该 spec 与其 evidence(diff 为空);成因是 #71 自己的
3b6f464 给四条判据都加了 timeoutMs,四个 criterionDigest 全变,却只重跑重录了其中两条。
即这是 #71 自己未完成的 evidence mutation,不是继承的旧债。若带着 2 PASS + 2 STALE 合入,
而 PR 正文写着「四条 native-lite 判据的首次全绿运行」,就又落入本线反复在治的病:
叙述范围大于落地证据范围。

补法是**重跑 machine verifier 并重录 evidence**,不是 attest —— 这四条是 command 类,
信任门只接受 manual,attest 会被直接拒绝(该守卫在本次操作中被实际走到)。
在干净的集成 head 1de7122 上 run,四条全部重绑 1de7122;原本 digest 就是最新的两条
digest 不变,原本 STALE 的两条 digest 刷新(a2ab4027→df526f94, 36c38c31→b370e36c)——
因果关系是从数据里显出来的,不是断言的。--strict 退出 0,4/4 PASS。
evidence-only commit 只含一个文件,提交后 status 仍 4/4(未碰 dependsOn 与判据定义)。

CI 纪律:34021030521 @ 1de7122 的绿只授权集成 head;evidence commit 之后 head 变为 82700c8,
必须重跑,由 34022390948 @ 82700c8 授权最终合并。

2.4.0 一致性:package.json / lock root / lock packages[""] 三处同为 2.4.0,
且 zvec 仍为 optionalDependencies 0.7.0,版本 bump 未改动 #66 的 dependency semantics。
#72 语义在合流后保留:manual 无 STALE 豁免、batch attest 仍是变参形式。

下一件真实 blocker 已明确:2.4.0 尚未交付到 npm(latest 仍是 2.3.0,发布于 2026-07-17)。
不为它开新的治理 gate。

## 架构决策 (Architecture)
未记录
