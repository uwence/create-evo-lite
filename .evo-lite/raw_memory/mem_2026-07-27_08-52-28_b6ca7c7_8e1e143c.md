---
id: "b6ca7c7_8e1e143c"
timestamp: "2026-07-27T08:52:28.451Z"
type: "task"
namespace: "prose"
tags: []
---

## 实现细节 (Implementation)
## 实现细节 (Implementation)
[governance-closure]
ATTP 宿主自有写入根(`[attp-guard-allowlist]`)在【设计 §2.1 的 SUPPORTED 拓扑】内收口。
spec:attp-host-owned-write-roots / plan:attp-host-owned-write-roots。
实现 main@5aa625f..014655f,验收与拓扑冻结 main@342dcd0..b6ca7c7。
两道外部复审门(生产实现复审、Task 8 验收复审)+ 一次拓扑冻结复审,均已 ACCEPTED。

契约:守卫的 target-path 门增加**一条并列允许根** —— 只由**当前事件**的 `transcript_path`
派生的 `dirname(transcript_path)/memory`。receipt 门与健康门**不放宽**;`Bash` 边界不变;
锚点缺失/畸形/不可解析时**不启用例外**,绝不退回宽白名单。

**支持拓扑(承重)**:单工作树仓库 / 独立项目副本 SUPPORTED;git linked worktree 在
Claude Code 2.1.220 下 UNSUPPORTED,mismatch 必须 fail-closed。缺口转
spec:attp-linked-worktree-memory-identity(parked / blocked-upstream)。
**`[attp-hive-rollout]` 不随本次收口解阻。**

## 完成任务

- task:attp-host-owned-write-roots-t1 — 新增 dependency-neutral 路径原语
  `templates/cli/takeover-physical-path.js`:`resolvePhysicalPath(target, fsOps)`
  返回「已物理验证前缀 + 回拼的未存在尾部」,并冻结 5 条 coded error taxonomy
  (`ATTP_NOT_ABSOLUTE` / `NO_EXISTING_ANCESTOR` / `BROKEN_LINK` / `REALPATH_FAILED`
  / `STAT_FAILED`,各带 `code/target/probe/cause`)。异常边界:无 `code` 的错误原样重抛
  (程序缺陷不得被伪装成路径判定)。main@5aa625f。

- task:attp-host-owned-write-roots-t2 — installer 文案 characterization,零生产改动。
  为 `resolveManagedSettingsPath` 的四条既有错误文案补上先前完全没有的回归覆盖,
  使 Task 3 的重构可被判定为「文案不漂移」。main@a3d9b42。

- task:attp-host-owned-write-roots-t3 — `takeover-install.js` 改用共享原语,
  并落地本议题唯一批准的**安全收紧**:非 ENOENT 的 lstat 失败由「吞掉并继续上溯」
  改为「抛 STAT_FAILED 拒绝触碰 settings」(设计 §6.2.1 勘误,`treat-as-missing` NOT ALLOWED)。
  installer **不** import receipt,两者只共享原语。main@1693073。

- task:attp-host-owned-write-roots-t4 — 守卫改用原语,把物理归一化从「最近存在祖先」
  换成「已验证前缀 + 回拼尾部」,对既有项目判定 verdict-preserving(既有 T-takeover-* 全套原样通过)。
  本任务**不引入**允许根。四条 code→文案映射补了逐字断言 + 消费计数
  (原实现的红-绿证明不成立,复审 P1 后重做)。main@33906fd。

- task:attp-host-owned-write-roots-t5 — `deriveHostOwnedWriteRoots(hookInput)` 落在
  `takeover-receipt.js`,返回 `{ok, reason, roots}`,永不向守卫外抛;失败时 `roots` 恒为 `[]`。
  只读当前事件的 `transcript_path`,不读环境变量 / settings / receipt。main@cb4804c。

- task:attp-host-owned-write-roots-t6 — 守卫 target-path 门增加**并列**允许根 +
  设计 §8 完整回归矩阵(其他项目 memory deny、前缀关系项目 memory deny、`<slug>` 下非
  memory deny、`memory/../escape` deny、memory 内 symlink 指向外部 deny、Windows 大小写与
  U+212A Unicode case-fold 变体不得扩大权限)。main@cdf6978。
  复审后补窄修订 main@014655f:证明**健康门不可被 memory 例外绕过**
  (committed receipt + 删 active_context → deny 且文案为 takeover unhealthy,恢复后同一目标重新 allow),
  并把 Unicode 能力探测改用与生产同源的 `rc.realpathStrict`(`fs.realpathSync.native`),
  避免纯 JS realpath 自行折叠大小写导致假 skip 掩盖折叠回归。

- task:attp-host-owned-write-roots-t7 — README / README_EN 把「项目外 Edit/Write 一律 deny」
  改为「默认 deny + 一条由当前事件派生的窄例外」,保留 `Bash` 可绕过与守卫非隔离边界两条声明;
  运行时镜像 `sync-runtime` 跑到固定点。main@80f5da9。
  拓扑冻结后再次同步(main@16fa181):双语 README 增加支持拓扑表、linked worktree 可能被安全拒绝、
  以及三条明确的「不要这样绕」(改路径大小写 / 自拼 slug 编码 / 用户级配置加根)。

- task:attp-host-owned-write-roots-t8 — 双会话真实记忆终局验收(**只产出证据**)。
  证据 `docs/validation/attp-guard-allowlist-acceptance.md`(main@342dcd0):
  Session A 3 次工具调用 ↔ 3 次 PreToolUse allow / 0 deny 一一对应,Bash 机械禁用;
  Session B 全新会话 0 次 Read/Glob/Grep/Bash 逐字返回唯一 marker,空 memory 项目负控 NO-MARKER;
  Session C 覆盖 `memory/` 已存在时的常规路径写入与 `Edit` 臂。
  同轮发现并如实上报:**宿主自己在 SessionStart 创建空 `memory/`**,故实景证明的是
  「首次 agent memory **文件**写入」而非目录创建;多级尾部再拼仅有自动化证据,保留为防御性下界。
  另发现 linked worktree 缺口,经 Step 0c 观测
  (`docs/validation/attp-guard-allowlist-step0c-worktree-memory-identity.md`,main@a0c73f0)
  判定终止分支 **B ∧ C**:hook 完整键集无 memory 锚点;Git identity 到 memory root 差一层
  未文档化且有损的 slug 编码;映射在路径大小写维度上失稳(按 Git identity 派生会**同时扩权与失效**)。
  据此冻结支持拓扑(main@16fa181 + 79b9e3d),Task 8 于 main@b6ca7c7 收口勾选。

## 架构决策 (Architecture)
未记录
