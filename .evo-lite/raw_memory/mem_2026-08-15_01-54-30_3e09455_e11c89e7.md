---
id: "3e09455_e11c89e7"
timestamp: "2026-08-15T01:54:30.991Z"
type: "task"
namespace: "prose"
tags: []
---

## 实现细节 (Implementation)
[HookStatusFreshness]
[235a] hook-status-freshness shipped and merged via PR #47 (merge 3e09455, reviewed head 975a67a, CI 6/6). mem hook status now consumes diffInstalledHook()'s existing freshness evidence and reports four states: no-hook / no-block / in-sync / drifted. Two deliberate behaviour changes, stated in the commit, a code comment and the test assertions: no-block moves from exit 0 to exit 1, and drifted exits 1 recommending mem hook diff before mem hook install. CRLF stays drift by contract - no normalization - because what is compared is whether the installed EXECUTABLE ARTIFACT still equals the canonical managed block, not whether the text reads the same. status remains a pure observer. Origin: after the disposition ledger merged, the installed post-commit hook predated it by two months and the feature never ran, while every gate and all CI were genuinely green. Post-merge host verification is the load-bearing item here: hook status reports installed and current with exit 0, and hook diff reports in-sync, on the real mother repo - and that wording is itself the new code, so the merged change is proven active on the host, not merely present in the tree. Post-merge full suite on 3e09455: EXIT 0, 439 blocks. Backlog [0ce0] verify-hook-runtime-health stays DESIGN-NEEDED and unimplemented; the three disposition-ledger known debts stay untouched.

## 架构决策 (Architecture)
未记录
