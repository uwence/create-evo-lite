---
id: "1108e9d_59a88a36"
timestamp: "2026-07-26T13:21:32.895Z"
type: "task"
namespace: "prose"
tags: []
---

## 实现细节 (Implementation)
[governance-closure]
ATTP (Agent Takeover Trigger Protocol) closure. spec:agent-takeover-trigger-protocol
[done], plan:agent-takeover-trigger-protocol 8/8. Backlog [agent-code-routing] resolved.
Implementation main@5985dff..16ee1cd via Subagent-Driven Development, two external
review gates, both PASSED.

## 完成任务

- task:agent-takeover-trigger-protocol-t1 — 纯函数 builder + 两个 discriminated
  validator(takeover-payload.js);capsule 恒 ≤1 KiB;emergency capsule 独立于正常
  builder,确定性降级阶梯,恢复命令只整条带上或整条省略。
- task:agent-takeover-trigger-protocol-t2 — receipt 层(takeover-receipt.js):严格根发现
  无 fail-open 兜底、project-bound 路径、session-scoped、ordered publication、失效事务、
  fs seam;判定与副作用分离(reconcile / reconcileReadOnly 共用同一分类函数)。
- task:agent-takeover-trigger-protocol-t3 — 单一 collector(takeover-session.js):bootstrap
  四件套 + plan/spec/freshness 派生 + 结构化降级(可恢复入 degraded[],不可恢复抛错)。
- task:agent-takeover-trigger-protocol-t4 — 生命周期 adapter + 两条 transport
  (takeover-adapter.js);宿主退出码契约:已序列化 envelope 一律 exit 0,失败由
  failure + systemMessage + stderr 表达(非零会让宿主丢弃整个 JSON)。
- task:agent-takeover-trigger-protocol-t5 — 三入口收口:mem bootstrap 经 builder +
  --receipt CLI recovery;恢复命令带 --session-id 且 sessionId 非法时不谎称可恢复。
- task:agent-takeover-trigger-protocol-t6 — installer 事务化 capability-gate + manifest +
  gitignore + 镜像 + 阶段一 dogfood/复审门;受管身份精确同一并下沉到 hook 条目级;
  备份/回滚/discard 三件套按 sha256 字节级校验。
- task:agent-takeover-trigger-protocol-t7 — PreToolUse fail-closed 守卫:完整 health gate +
  target-path 绑定;任何异常一律 deny;路径归一化用 realpathSync.native 真实大小写。
- task:agent-takeover-trigger-protocol-t8 — 阶段二故障注入验收(八条用例:1–7 注入,
  8 端到端恢复)+ 复审门 2 + PreToolUse 安装 + README 首次成文 + 备份 discard。

## 复审门证据

Gate 1(Tasks 1–6,三轮):P0-1 子目录启动实测不接管 → 范围收窄为 **root-launch-only**,
原验收项撤销;实测发现两条各自独立、都锚定启动 cwd 的机制(项目 settings 不向上查找;
$CLAUDE_PROJECT_DIR 展开为启动 cwd),因此复审建议的 --settings workaround **不成立**
(hook 会加载但每轮真实报错)。P1-1 isManagedGroup 子串匹配会误删同名第三方 hook →
改精确同一,并额外发现整组过滤会连累同组第三方 hook → 过滤下沉到条目级。
修订复审 P1-1 hooks 容器/事件值类型未校验(hooks: [] 会假成功)→ assertManagedHooksShape。

Gate 2(Tasks 7–8,四轮):Task 7 P1-1 已知 Windows Unicode 逃逸未进正式套件,且 skip 条件
把大小写敏感性等同于 OS 名 → 加 U+212A KELVIN SIGN fixture(写作 K 转义,裸字形一次
编辑器归一化就会退化成恒真),skip 改按文件系统实际 alias 行为判定。此前更严重的一环:
为修 win32 drive-case 误拒而引入的 toLowerCase() 折叠范围大于 NTFS,导致真实越界写出项目外
——改为两侧同经 realpathSync.native 的真实大小写比较。Gate 2 P1-1 用例 4 的注入消费未锁进
回归(删掉 UserPromptSubmit 调用后两个 seam 零调用而四条断言仍全过)→ 加 seam 调用计数;
用例 4/7 的伪 deny(focus 为 null → capsule 空指针 → 内层 catch 兜成 deny)→ 补文案断言。

## Step 5 收口

node .evo-lite/cli/memory.js takeover backup-discard → exit 0。后置确认:备份文件不存在、
manifest 不存在、takeover status 三事件仍在位、.claude/settings.json sha256 bb8a89b8 在
discard 前后一致、.evo-lite/generated/takeover/ 只剩 receipts/ 无孤儿。

## 门

node templates/cli/test.js all EXIT 0(215 项,PreToolUse 在位);mem sync-runtime 二次运行
copied: 0 / unchanged: 119;实景守卫三门:无 receipt → deny "takeover required"、
真实会话项目内 → allow、项目外 → deny "resolves outside project",均 exit 0 且 stdout 只有 JSON。

## Rollout 注意

(1) 子仓分发是独立议题 [attp-hive-rollout],未授权;本 MVP 只保证 installer 幂等可用。
(2) 装上守卫后项目外 Edit/Write 会被 deny,agent 临时文件必须落在项目内。
(3) 母仓 settings.json 无第三方 hook,「第三方 hook 共存」只有自动化回归证据,无实景观察。
(4) 计划里那条「凡 __setFsOps 用例都必须加 seam 计数」措辞过严;准确原则是:每个 seam 的
消费必须有能杀死「seam 未被调用」变异体的承重断言,下游状态足以证明消费时不必额外计数。

## 架构决策 (Architecture)
未记录
