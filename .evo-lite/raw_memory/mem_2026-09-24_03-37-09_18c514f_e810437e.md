---
id: "18c514f_e810437e"
timestamp: "2026-09-24T03:37:09.890Z"
type: "task"
namespace: "prose"
tags: []
---

## 实现细节 (Implementation)
[plan lifecycle authority adopted]
PR #77 merged into main as merge commit 18c514f (parents 5a3672b + 5317f16 exact adopted head, --match-head-commit; CI 6/6 SUCCESS). spec:plan-lifecycle-authority is ADOPTED; Phase A contract FROZEN after three owner review rounds (revisions 92a3b42, 362125d, 8d93f9c; adoption 5317f16 was frontmatter-only).

Frozen contract: plan status is authored-only (absent -> unknown/unspecified; explicit unknown -> authored); taskCompletion (complete/incomplete/no-tracked-work) is observed and never moves lifecycle; derived-done rejected. Every close path writes lifecycle only; the verification-close checkbox rewrite is retired (supersedes in part verification-contract-phase3 flip step and closure-correctness ac-plan-status-done). §5.1 active-plan eligibility: authored active, then authored draft; unknown/parked/done/unrecognized never; ties by plan-IR order; in_progress retired. Unrecognized status = scan warning only, no rule.

Migration boundary: Phase B census = parser population (24) ∪ eligibility population (2: plan:governance-observation-budget, plan:planning-truth-controls, authored active, specs closed-record-only) = 26; decisions `target <recognized-status>` or `leave unknown` (only for plans with no authored status), exactly one per plan, set equality checked. Phase C is one atomic change: approved writes -> parser switch -> consumer reconciliation.

NOT authorized by this approval: Phase B, C, D implementation (each needs its own authorization per §6). The two authored-active plans are NOT decided; they belong to the Phase B census.

Post-merge verify on main@18c514f: EXIT=0, adopted=1 (the new spec, no linked plan), recordClosed=3, findings unchanged at 5 (3 zombie-plan on draft plans, 2 time-driven aging).

## 架构决策 (Architecture)
未记录
