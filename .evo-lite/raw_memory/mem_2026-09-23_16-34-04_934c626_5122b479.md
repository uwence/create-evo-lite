---
id: "934c626_5122b479"
timestamp: "2026-09-23T16:34:04.114Z"
type: "task"
namespace: "prose"
tags: []
---

## 实现细节 (Implementation)
[post-merge state closure]
PR #75 (spec/record-only-closure-terminal-state) merged into main as merge commit 934c626 (parents 5c091b9 old main + 066fb6c exact reviewed head, merged with --match-head-commit; CI 6/6 SUCCESS incl. windows/node 22+24 after ce830a9 leak fix).

Two Backlog Ideas defects are now FIXED on main and removed from Backlog Ideas (that section forbids accumulating completed items):

[spec-size-gate-state-blindness] FIXED merged@934c626 — spec-portfolio.js now separates measurement (sizeExceeded, every state) from actionability (SIZE_ACTIONABLE_STATES = {adopted, active}). Evidence on main: `mem verify` no longer flags the shipped/parked specs from the 2026-08-09 measurement; the only size-exceeded finding is spec:disposition-ledger (active), and the message now prints the actually-breached dimension (acCount=9 > 8). Scope note: gate covers adopted as well as active (both in-flight), slightly wider than the original "active only" suggestion. sizeWaiver was NOT used to silence anything.

[spec-zombie-plan-parked-deadlock] FIXED merged@934c626 — zombie-plan now uses its own ZOMBIE_SETTLED_PLAN_STATUSES = {done, parked}, distinct from the notDonePlans predicate aging-inactive reads. Evidence on main: spec:unified-code-explore-wiki-projection (parked) x plan:code-wiki-inspector-projection (parked) no longer warns; the 3 remaining zombie-plan findings (codegraph-adapter-governance-linker, evidence-durability-stale-cascade, mother-child-hive-nurture) all link plans in status=draft, i.e. genuinely unsettled — correct findings, not residue.

Post-merge verify on main@934c626: EXIT=0, recordClosed=0, 8 findings = the 6 expected at 066fb6c + 2 purely time-driven aging findings (r011-closure-router idle 19d, portfolio-finding-correctness idle 15d; DEFAULT_AGING_DAYS=14, 15 days elapsed since 2026-09-09).

NOT done and NOT authorized by this closure: applying closed-record-only to any of the three historical specs — each use needs its own adjudication and closureReason (spec A Follow-ups). Candidate backlog noted but not registered here: narrow the EBUSY amnesty to the pre-existing verify-spec-portfolio-clean case only.

## 架构决策 (Architecture)
未记录
